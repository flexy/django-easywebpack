============
Installation
============

At the command line::

    $ easy_install django-easywebpack

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-easywebpack
    $ pip install django-easywebpack
